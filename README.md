# Lehrlinge Portal

Separate static portal for Lehrlinge. The project can be deployed as a standalone Vercel project with this directory as its repository root.

## Local preview

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173/` from inside this directory.
